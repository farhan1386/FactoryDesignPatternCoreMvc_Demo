@model FactoryDesignPatternCoreMvc_Demo.Models.EmployeeType

@{
    ViewData["Title"] = "Details";
}

<div class="card">
    <div class="card-header">
        <h4 class="text-uppercase">Employee Type Details</h4>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(m => m.EmployeeTypeName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(m => m.EmployeeTypeName)
            </dd>
        </dl>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-sm btn-info rounded-0"><i class="fas fa-pencil-alt"></i> Edit</a>
            <a asp-action="Index" class="btn btn-sm btn-primary rounded-0"><i class="fas fa-backward"></i> Back to List</a>
        </div>
    </div>
</div>
